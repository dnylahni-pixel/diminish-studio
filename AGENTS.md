# AGENTS.md — قوانین کاری (MUST follow)

> این فایل برای هر agent/AI که در این ریپو کار می‌کند الزامی است.
> قبل از هر اقدام، انجام هر تسک، یا پاسخ به هر درخواست مرتبط با پروژه،
> باید این فایل را کامل خوانده باشی.
> قانون پرسش انتخاب context حذف شده است؛ agent باید contextهای مرتبط با تسک
> را خودش شناسایی و کامل بخواند و کار را شروع کند.

> ⚠️ **SUB-AGENT OVERRIDE — برای ساباجنت‌ها اجباری است:**
> اگر این پیام/تسک را از agent اصلی (root) دریافت کرده‌ای، یعنی **ساباجنت** هستی.
> هرگز از کاربر سوال نپرس و به‌خاطر قوانین شروع گفتگو متوقف نشو. فقط فایل‌های
> فهرست‌شده در تسک خودت را کامل بخوان و تسک را شروع کن. (بخش ۶ بر بقیه مقدم است.)

## ۰) قانون طلایی کنترل رفتار (MUST — قبل از هر تسک)

> این قانون برای همه agentها، همه تسک‌ها و همه قابلیت‌های جدید الزامی است؛
> هیچ مدل/agent دیگری حق نادیده‌گرفتن آن را ندارد.

- هر رفتار قابل مشاهده در User Application — دکمه، پیام، متن، مسیر/منو، عدد/حد، toggle،
  سرویس خارجی — باید از روز اول یک **کد پایدار** (مثل `import_url.enabled`) داشته باشد
  و مقدارش از **منبع واحد کانفیگ** (کاتالوگ «کنترل رفتار» / تنظیمات) خوانده شود؛
  hardcode در کامپوننت، route یا فایل ثابت ممنوع است.
- قابلیت جدید بدون ثبت «سطح کنترل‌پذیر» آن در کاتالوگ رفتار
  (فعال/غیرفعال، متن، ظاهر، مسیر) ساخته نمی‌شود.
- رفتارهای موجودِ سخت‌کد باید تدریجی به کاتالوگ «کنترل رفتار» منتقل شوند
  (مرجع: `docs/APPLICATION-BEHAVIOR-CONTROL-RESEARCH.md` +
  `docs/BEHAVIOR-CONTROL-MASTER-LIST.md`).
- استثنا: اسرار، magic-bytes و گاردهای امنیتی در کد می‌مانند؛
  فقط «مقدار» قابل تنظیم است، نه منطق.
- منبع مؤلفه‌های UI برای ادمین جدید **BEUI** است
  (`apps/admin-new/src/components/beui/` + مستندات beui.dev / MCP beui)؛
  کامپوننت‌های Appica برای صفحه‌های جدید استفاده نمی‌شوند.

## ۱) شروع کار — بدون پرسش انتخاب context

- این فایل را کامل بخوان.
- **هرگز از کاربر نخواه شماره‌ی context بفرستد و منتظر انتخابش نمان.** این
  پرسش اولیه به‌طور کامل حذف شده است.
- contextهای مرتبط با تسک را **خودت** شناسایی و کامل بخوان، سپس کار را شروع کن:

| اگر تسک … | این فایل‌ها را کامل بخوان |
| --- | --- |
| طراحی/ساخت UI در admin-new | `docs/admin-ui-rebuild/THEME-GUIDE.md` (مرجع اصلی سازنده‌ها)، `apps/admin-new/src/components/lab/CONVENTIONS.md` (مکانیک لاب)، منبع کامپوننت‌ها = **BEUI** (`apps/admin-new/src/components/beui/` + مستندات beui.dev / MCP beui) و قرارداد بک‌اند (`queries/actions/types` + `db/schema`) |
| کار روی دامنه/اجرای قوانین | `CONTEXT.md`، `docs/adr/`، `docs/admin-user-integration/00-INDEX.md` |
| کار روی داده/قراردادها | schemaهای مربوطه (`packages/db` / `apps/admin/src/db`)، و `packages/api-spec/openapi.yaml` در صورت نیاز |
| تسک کلی/نامشخص | `CLAUDE.md` (نقشه‌ی سریع ریپو) + مرتبط‌ترین فایل از فهرست بخش ۲ |

- اگر تسک چند حوزه را درگیر می‌کند، فایل‌های مرتبطِ همه‌ی حوزه‌ها را بخوان
  (معادل «همه» در فهرست قدیمی).
- فقط وقتی تصمیم محصولی/فلویی واقعاً نیاز به نظر مالک دارد بپرس — نه برای
  انتخاب context.

## ۲) فهرست contextها (برای هدایت خودکار)

1. `CLAUDE.md` — نقشه‌ی سریع ریپو، دستورها و قواعد کلی.
2. `CONTEXT.md` — زبان مشترک و دامنه‌ی پروژه (Plan Enforcement).
3. `readme.md` — معرفی کلی پروژه.
4. `docs/admin-ui-rebuild/00-INDEX.md` — قوانین بازسازی کامل ادمین و منبع حقیقت UI جدید.
5. `docs/admin-ui-rebuild/THEME-GUIDE.md` — مرجع اصلی طراحی/تم و فرایند ساخت UI.
6. `apps/admin-new/src/components/lab/CONVENTIONS.md` — مکانیک Review Lab / آزمایشگاه.
7. `docs/admin-user-integration/00-INDEX.md` — معماری اتصال Admin به User Application.
8. `docs/PROJECT-UNIFICATION-ROADMAP.md` — نقشه‌ی راه یکپارچه‌سازی.
9. `docs/adr/` — تصمیم‌های معماری (ADRs).
10. `apps/admin/src/features/*/queries.ts` + `actions.ts` + `types.ts` — قرارداد بک‌اند ادمین.
11. `apps/admin/src/db/schema/` — اسکیمای واقعی دیتابیس ادمین.
12. `packages/api-spec/openapi.yaml` — قرارداد OpenAPI اپ کاربر.
13. `packages/db/src/schema/` — اسکیمای دیتابیس اپ کاربر.

## ۳) بعد از خواندن contextها

- فایل‌های مرتبط را **کامل** بخوان، نه فقط اولشان. اگر پوشه‌ای انتخاب شد،
  فایل‌های مرتبط داخل آن را شناسایی و کامل بخوان.
- در یک پیام کوتاه بگو کدام contextها را خوانده‌ای و سپس **بدون انتظار برای
  تأیید اضافه**، تسک را شروع کن؛ مگر اینکه خود تسک صریحاً به تصمیم مالک نیاز
  داشته باشد.

## ۴) اگر ابهام واقعی وجود داشت

- فرض منطقی بزن و جلو برو. اگر فرض باعث تغییر معنادار دامنه‌ی تسک می‌شود،
  همان موضوع را کوتاه با کاربر در میان بگذار — نه فهرست انتخاب context.

## ۵) مواردی که مشمول این قانون نیستند

- سوال‌های صرفاً متا درباره‌ی خود agent یا ابزار.
- درخواست‌هایی که صریحاً به فایل‌های ریپو دست نمی‌زنند (مثلاً سوال عمومی).

## ۶) قانون ساباجنت‌ها (sub-agents)

- ساباجنت یعنی agentای که توسط agent اصلی (root) برای یک تسک مشخص و محدود
  صدا زده می‌شود؛ نه گفتگوی مستقیم با کاربر.
- ساباجنت‌ها از هر پرسشی از کاربر معاف‌اند و نباید به‌خاطر قوانین شروع گفتگو
  متوقف شوند یا دوباره از کاربر سوال بپرسند.
- ساباجنت باید AGENTS.md را کامل بخواند، اما فقط فایل‌هایی را که در متن تسکش
  فهرست شده‌اند کامل بخواند و کار را شروع کند.
- اگر فهرست فایل‌ها در تسک ناقص بود، ساباجنت می‌تواند از agent اصلی توضیح
  بخواهد — نه از کاربر.
- بقیه‌ی قواعد ریپو همچنان برای ساباجنت الزامی است: قرارداد بک‌اند منبع حقیقت
  است (خواندن `queries/actions/types` و schema مجاز؛ خواندن یا کپی UI قدیمی
  ادمین ممنوع)، تغییر `apps/admin` فقط با تأیید صریح مالک محصول، و گزارش هر
  تغییری که انجام می‌دهد.
