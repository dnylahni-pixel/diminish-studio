# DiminishStudio — Weekly Challenge

## فیچر ۳: چالش هفتگی

-----

## خلاصه یه خطی

هر هفته یه آهنگ مطرح ایرانی انتخاب میشه — کاربرا کاورش میکنن — جامعه رای میده — نفر اول جایزه میگیره.

-----

## چرا این growth engine پلتفرمه

```
یه آهنگ هفتگی
↓
کاربر کاور میکنه → publish میکنه → به دوستاش میگه "بیا رای بده"
↓
دوست میاد سایت → ثبت‌نام میکنه → خودشم میخونه
↓
اون هم به دوستاش میگه
↓
loop
```

این **ارگانیک‌ترین growth loop ممکنه** — بدون هزینه تبلیغات.

-----

## فلوی کاربر

```
صفحه اصلی → بنر "چالش این هفته" همیشه visible
↓
کلیک → صفحه چالش
↓
پیش‌نمایش آهنگ اصلی + لیست شرکت‌کننده‌ها
↓
دو حالت:
[تماشا] → scroll کاورها → لایک بزن → کامنت بذار
[شرکت] → mute stem → ضبط → AI remix → submit
↓
لیدربورد real-time بر اساس لایک
↓
آخر هفته → برنده اعلام میشه → جایزه
```

-----

## صفحه چالش — ساختار UI

```
┌─────────────────────────────────────┐
│ 🏆 چالش هفته | ۵ روز مونده │
│ "شب که میشه" — محسن یگانه │
│ [گوش بده] [شرکت کن] │
├─────────────────────────────────────┤
│ 🥇 علی رضایی ۲۴۳ لایک │
│ 🥈 سارا محمدی ۱۸۷ لایک │
│ 🥉 رضا کریمی ۱۵۲ لایک │
│ ───────────────────────────────── │
│ ۴. دانیال احمدی ۹۸ لایک │
│ ۵. نیلوفر خانی ۷۶ لایک │
│ ... │
│ [همه ۱۲۴ شرکت‌کننده] │
└─────────────────────────────────────┘
```

-----

## معماری فنی

### جداول دیتابیس

```sql
challenges
- id serial PK
- song_id int FK→songs
- title varchar
- starts_at timestamp
- ends_at timestamp
- status varchar (upcoming/active/ended)
- winner_cover_id int FK→covers
- created_at timestamp

challenge_entries
- id serial PK
- challenge_id int FK→challenges
- cover_id int FK→covers
- user_id int FK→users
- like_count int default 0
- rank int (محاسبه‌شده)
- submitted_at timestamp

challenge_votes
- id serial PK
- challenge_id int FK→challenges
- entry_id int FK→challenge_entries
- voter_id int FK→users
- created_at timestamp
```

### API Endpoints

```
GET /api/challenges/active → چالش جاری
GET /api/challenges/:id/leaderboard → لیدربورد
POST /api/challenges/:id/submit → ارسال کاور
POST /api/challenges/:id/vote → رای دادن
GET /api/challenges/history → آرشیو چالش‌های قبلی
```

### Anti-Fraud (مقابله با رای فیک)

```javascript
// هر یوزر فقط یه رای به هر entry
UNIQUE(voter_id, entry_id)

// هر IP حداکثر ۵ ثبت‌نام در روز
rate_limit: 5 signups/IP/day

// اکانت باید حداقل ۲۴ ساعت قدمت داشته باشه
account_age_min: 24h for voting

// رای‌های burst (مثلاً ۵۰ رای در ۱ دقیقه) = flag میشه
vote_velocity_check: max 10 votes/minute/IP
```

> نکته: رای فیک = ثبت‌نام انبوه = پلتفرم رشد میکنه 😂
> فقط از برنده شدن با رای فیک جلوگیری کن، نه از ثبت‌نام.

-----

## سیستم جایزه

```
🥇 اول: ۲۰ پردازش رایگان + بج "چمپیون" روی پروفایل
🥈 دوم: ۱۰ پردازش رایگان
🥉 سوم: ۵ پردازش رایگان
همه شرکت‌کننده‌ها: +۲ سکه (فقط برای شرکت)
```

### چرا “پردازش رایگان” بهتر از پول نقده

- هزینه واقعی برای تو نزدیک صفره
- کاربر برنده دوباره میاد از جایزه‌ش استفاده کنه
- حس ارزشمندی بیشتری داره تا ۵۰ تومن

-----

## تقویم هفتگی

```
شنبه ۰۰:۰۰ → چالش جدید شروع میشه
→ notification به همه کاربرا
→ بنر سایت آپدیت میشه

طول هفته → leaderboard real-time آپدیت میشه
→ reminder notification روز چهارم

جمعه ۲۳:۵۹ → چالش بسته میشه
→ رای‌گیری قفل میشه

شنبه ۰۰:۰۰ → برنده اعلام میشه
→ notification به برنده
→ پست در explore: "برنده این هفته 🏆"
→ چالش جدید شروع میشه
```

-----

## چطور آهنگ هفته انتخاب بشه

### فاز اول (MVP)

خودت انتخاب میکنی — آهنگ‌های مطرح که مردم میشناسن.
معیار: آهنگایی که همه میخوان بخوننشون ولی جرئت ندارن.

### فاز دوم

```
کاربرا پیشنهاد میدن → رای‌گیری عمومی → آهنگ پررای = چالش هفته
```

این خودش یه engagement loop جداست.

-----

## Viral Mechanics

### ۱. Share Card

وقتی کاور submit میشه، یه کارت اتوماتیک ساخته میشه:

```
┌────────────────────────┐
│ 🎵 من توی چالش │
│ DiminishStudio │
│ شرکت کردم! │
│ │
│ "شب که میشه" │
│ محسن یگانه │
│ │
│ بیا رای بده 👇 │
│ diminish.studio/c/47 │
└────────────────────────┘
```

این کارت مستقیم برای استوری اینستاگرام سایز میشه.

### ۲. “دوستت شرکت کرده”

وقتی کاربر جدید از لینک میاد:

> “علی رضایی دوستته؟ اون این هفته شرکت کرده — بیا رای بده یا خودت هم بخون!”

### ۳. آرشیو

همه چالش‌های قبلی searchable میمونن.
برنده‌ها یه بج دائمی روی پروفایلشون دارن.
این انگیزه بلندمدت ایجاد میکنه.

-----

## MVP — چی لازمه برای لانچ

```
✅ صفحه چالش با لیدربورد
✅ submit کاور (از فیچر ۱ استفاده میکنه)
✅ vote با یه کلیک
✅ countdown timer
✅ اعلام برنده
✅ share card

⬜ notification (بعداً)
⬜ پیشنهاد آهنگ توسط کاربر (بعداً)
⬜ live leaderboard با websocket (بعداً - polling کافیه)
```

-----

## یه نکته استراتژیک مهم

اولین ۵ چالش رو با آهنگایی انتخاب کن که **خودت** میتونی کاورشون کنی.

اگه سازنده پلتفرم هم شرکت کنه و خوب بخونه:

- اعتماد ایجاد میشه
- استاندارد کیفیت مشخص میشه
- “اگه اون میتونه من هم میتونم” effect

-----

*“هر هفته یه صحنه. هر هفته یه برنده.”*
*DiminishStudio Weekly Challenge*