# Diminish Plan Enforcement

این context زبان مشترک اتصال Catalog مدیریتی به اجرای قابلیت‌ها در User Application را تعریف می‌کند.

## Scope

- هدف پروژه اتصال User Application به سیستم مدیریتی موجود است.
- بازطراحی محصول، منوها و تجربه کاربری Admin هدف اصلی این پروژه نیست، اما در صورت نیاز و با تأیید صریح مالک محصول مجاز است.
- تصمیم‌های این context فقط باید Interface و Seam لازم میان Admin و User Application را مشخص کنند.
- هیچ تغییر کد، schema، migration، تنظیمات یا داده در Admin بدون تأیید صریح مالک محصول مجاز نیست.

## Language

**قابلیت**:
یک امکان محصولی مستقل که مالک محصول آن را در Admin ایجاد، نام‌گذاری، فعال یا غیرفعال، قیمت‌گذاری و به پلن‌ها متصل می‌کند.
_پرهیز_: Route، endpoint، تابع

**تعریف قابلیت**:
اطلاعات مدیریتی قابلیت شامل کد پایدار، نوع، واحد مصرف، وضعیت، محدودیت، قیمت و وابستگی‌ها که در Admin نگهداری می‌شود.
_پرهیز_: پیاده‌سازی قابلیت

**عملکرد قابلیت**:
رفتار واقعی نرم‌افزار که نتیجه موردنظر قابلیت را تولید می‌کند و فقط هنگام ساخت یک عملکرد واقعاً جدید نیازمند کدنویسی است.
_پرهیز_: تعریف قابلیت

**عملیات اجرایی**:
یک عمل مشخص و قابل فراخوانی از عملکرد قابلیت که برای اعمال دسترسی، اندازه‌گیری مصرف و ثبت نتیجه به یک قابلیت اصلی متصل می‌شود.
_پرهیز_: قابلیت محصول

**دریچه قابلیت**:
محل اتصال استاندارد عملکرد قابلیت به زیرساخت عمومی احراز دسترسی، محدودیت، اعتبار، ثبت مصرف و بازیابی خطا.
_پرهیز_: منطق اختصاصی پلن داخل Route

**موتور اعمال قوانین**:
زیرساخت عمومی User Application که تعریف قابلیت را از Catalog می‌خواند و بدون شناخت عملکرد واقعی، دسترسی، محدودیت، قیمت، اعتبار و مصرف را اعمال می‌کند.
_پرهیز_: موتور مخصوص Analyze یا Upload

**درگاه اعمال قوانین**:
Interface واحدی که User Application برای دسترسی به تصمیم‌های پلن، محدودیت، اعتبار و مصرف می‌شناسد و روش اتصال واقعی را پشت خود پنهان می‌کند.
_پرهیز_: دسترسی مستقیم Routeها به جدول‌های Admin

**API اتصال مدیریتی**:
API اختصاصی و محدود میان User Application و سیستم مدیریتی که قراردادهای قابلیت، پلن، اعتبار و مصرف را بدون نمایش schema یا credential دیتابیس ارائه می‌کند.
_پرهیز_: API عمومی مرورگر

**Module اتصال مدیریتی**:
بخش مستقل داخل پروژه Admin که API اتصال مدیریتی را ارائه می‌کند و طوری طراحی می‌شود که بعداً بدون تغییر قرارداد به سرویس جدا منتقل شود.
_پرهیز_: منطق اتصال پراکنده داخل صفحه‌ها و actionهای Admin

**Adapter API اتصال**:
پیاده‌سازی تولیدی درگاه اعمال قوانین که فقط از API اتصال مدیریتی استفاده می‌کند.
_پرهیز_: Adapter دیتابیس مشترک در production

**هویت سرویس**:
هویت کوتاه‌عمر و قابل چرخش User Application برای ارتباط سرور‌به‌سرور با API اتصال مدیریتی.
_پرهیز_: API key دائمی، توکن مرورگر

**مجوز اجرا**:
پاسخ API اتصال مدیریتی که اجازه اجرای یک عملیات مشخص را پس از بررسی کاربر، پلن، قابلیت، محدودیت و اعتبار اعلام می‌کند.
_پرهیز_: تصمیم محلی Route

**رزرو مصرف**:
ثبت موقت و idempotent اعتبار یا سهمیه پیش از اجرای عملکرد واقعی که بعداً قطعی یا آزاد می‌شود.
_پرهیز_: کسر قطعی پیش از نتیجه عملکرد

**نسخه منتشرشده قوانین**:
نسخه پایدار و قابل اجرای تنظیمات قابلیت، پلن، محدودیت، قیمت و وابستگی که مالک محصول پس از پایان ویرایش آن را منتشر می‌کند.
_پرهیز_: پیش‌نویس در حال ویرایش

**همگام‌سازی دستی قوانین**:
عمل صریح مالک محصول برای انتقال نسخه منتشرشده قوانین به وضعیت قابل استفاده User Application.
_پرهیز_: فعال‌شدن خودکار تغییرات Admin

**بسته همگام‌سازی**:
مجموعه انتخابی و کامل یک یا چند پلن و قابلیت که به‌صورت atomic همگام می‌شود و حالت نیمه‌منتقل‌شده ندارد.
_پرهیز_: انتقال ردیف‌به‌ردیف ناقص

**پیش‌نمایش همگام‌سازی**:
نمایش تغییر قیمت، محدودیت و تعداد کاربران تحت‌تأثیر پیش از تأیید نهایی مالک محصول.
_پرهیز_: همگام‌سازی بدون نمایش اثر

**بازگردانی قوانین**:
بازگشت کنترل‌شده از بسته همگام‌سازی فعلی به نسخه همگام‌شده قبلی.
_پرهیز_: ویرایش دستی برای شبیه‌سازی نسخه قبلی

**توقف هنگام به‌روزرسانی**:
گزینه مدیریتی برای توقف موقت قابلیت نزد تمام کاربران مشمول تا پایان اصلاح و همگام‌سازی قوانین آن.
_پرهیز_: غیرفعال‌شدن ناخواسته هنگام ویرایش پیش‌نویس

**نسخه قرارداد اتصال**:
نسخه صریح Interface میان User Application و API اتصال مدیریتی که امکان پشتیبانی هم‌زمان نسخه قبلی و جدید را فراهم می‌کند.
_پرهیز_: تغییر ناسازگار بدون نسخه

**شناسه عملیات**:
شناسه یکتای اجباری برای یک اجرای هزینه‌دار یا مصرفی که retry همان درخواست را از مصرف و هزینه دوباره محافظت می‌کند.
_پرهیز_: شناسه تصادفی تازه در هر retry

**گزارش نتیجه عملیات**:
پیام idempotent موفقیت یا شکست عملکرد واقعی که برای قطعی‌سازی یا آزادسازی رزرو مصرف ارسال می‌شود.
_پرهیز_: اجرای دوباره عملکرد برای جبران خطای گزارش

**نتیجه دودویی عملیات**:
نتیجه قابل مشاهده عملیات که تا حد امکان فقط موفق یا ناموفق است و حالت میانی را به‌عنوان نتیجه محصول نشان نمی‌دهد.
_پرهیز_: نتیجه نیمه‌موفق مبهم

**تلاش خارجی پرهزینه**:
فراخوانی عملکرد خارجی که حتی در صورت شکست برای سیستم هزینه ایجاد می‌کند.
_پرهیز_: retry رایگان

**هزینه واقعی عملیات**:
هزینه‌ای که اجرای عملیات واقعاً به سیستم تحمیل کرده است، مستقل از موفقیت، شکست یا مبلغ دریافت‌شده از کاربر.
_پرهیز_: قیمت فروش به کاربر

**زیان جذب‌شده شکست**:
هزینه واقعی عملیات ناموفق که سیستم پرداخت کرده ولی به دلیل شکست از کاربر دریافت نشده است.
_پرهیز_: بدهی کاربر

**سقف retry شکست**:
آستانه قابل تنظیم Admin برای برجسته‌کردن شکست‌های پرهزینه و کمک به تصمیم دستی درباره اجازه تلاش دوباره.
_پرهیز_: تعداد ثابت retry

**سقف زیان کاربر**:
آستانه تجمعی قابل تنظیم Admin برای برجسته‌کردن کاربرانی که شکست‌هایشان زیان جذب‌شده زیادی ایجاد کرده است.
_پرهیز_: جریمه خودکار کاربر

**توقف بررسی کاربر**:
وضعیت موقت جلوگیری از عملیات هزینه‌دار که فقط Admin پس از بررسی انسانی می‌تواند آن را فعال یا غیرفعال کند.
_پرهیز_: مسدودی دائمی خودکار

**افزونه آزمایشی**:
دسترسی موقت به قابلیت‌های آزمایشی که بدون جایگزین‌کردن پلن اصلی به کاربر منتخب اضافه می‌شود.
_پرهیز_: تعویض مخفی پلن اصلی

**قانون تعلق در بدو ورود**:
تنظیم مدیریتی که مشخص می‌کند یک دسترسی، پلن یا مزیت تعیین‌شده هنگام ورود اولیه کاربر به‌صورت خودکار به او تعلق بگیرد یا نه.
_پرهیز_: تخصیص خودکار hard-coded در User Application

**فهرست اتصال Runtime**:
فهرست امن و خودکار عملیات اجرایی واقعاً ثبت‌شده در User Application که Admin برای نمایش وضعیت اتصال آن را بررسی می‌کند.
_پرهیز_: اعلام دستی اتصال

**پیش‌فاکتور عملیات**:
محاسبه هزینه عملیات پیش از اجرای واقعی که برای مشاهده کاربر برگردانده می‌شود.
_پرهیز_: محاسبه هزینه پس از اجرای عملکرد

**اثر انگشت عملیات**:
شناسه تغییرناپذیر مسیر اجرای دقیق که از کاربر، عملیات، منبع واقعی، محتوای منبع، تنظیمات و نسخه قوانین ساخته می‌شود.
_پرهیز_: نام فایل یا ورودی قابل تغییر

**تأیید پیش‌فاکتور**:
تأیید صریح کاربر برای اجرای عملیات هزینه‌دار بر اساس پیش‌فاکتور نمایش‌داده‌شده.
_پرهیز_: اجرای خودکار عملیات هزینه‌دار

**پیش‌فاکتور تک‌مصرف**:
پیش‌فاکتوری که فقط برای یک اثر انگشت عملیات معتبر است و پس از تأیید، اجرا، لغو یا تغییر هر ورودی دیگر قابل استفاده نیست.
_پرهیز_: پیش‌فاکتور معتبر برای چند فایل یا چند اجرا

**مجوز زمان‌دار**:
مجوز اجرای دارای زمان انقضا که عملیات طولانی می‌تواند با پیام زنده‌بودن معتبر نگه دارد.
_پرهیز_: مجوز دائمی

**سقف اعتبار منفی**:
مقدار بدهی اعتباری مجاز که مالک محصول برای هر پلن تعیین می‌کند و عملیات نمی‌تواند موجودی را پایین‌تر از آن ببرد.
_پرهیز_: تخفیف، اعتبار رایگان

**شناسه منبع عملیات**:
شناسه پایدار فایل، پروژه یا منبعی که برای جلوگیری از اجرای هم‌زمان تکراری استفاده می‌شود.
_پرهیز_: شناسه عملیات

**کد دلیل**:
کد ثابت و قابل پردازش برای توضیح نتیجه اجازه یا خطا بدون نمایش جزئیات داخلی و اطلاعات شخصی.
_پرهیز_: متن خطای آزاد حاوی داده حساس

**کد پایدار قابلیت**:
شناسه فنی دائمی قابلیت که پس از استفاده تغییر یا حذف نمی‌شود و فقط قابلیت می‌تواند archive شود.
_پرهیز_: نام نمایشی قابلیت

**پلن اصلی**:
پلن پایه و یکتای هر کاربر که دسترسی‌های اضافه بدون جایگزینی آن از طریق افزونه‌ها داده می‌شوند.
_پرهیز_: چند پلن اصلی هم‌زمان

**نتیجه معتبر**:
خروجی واقعی، کامل و قابل استفاده عملیات که تنها پس از تولید آن هزینه کاربر قطعی می‌شود.
_پرهیز_: پاسخ خارجی ناقص یا غیرقابل استفاده

**پرونده بازگشت اعتبار**:
پرونده auditشده یک نتیجه مشکوک یا خراب که سیستم برای بررسی Admin ایجاد می‌کند، بدون انجام خودکار refund.
_پرهیز_: refund خودکار

**قابلیت تعریف‌شده**:
قابلیتی که مالک محصول آن را در Admin ساخته است، حتی اگر هنوز عملکرد واقعی یا اتصال اجرایی نداشته باشد.
_پرهیز_: قابلیت آماده استفاده

**قابلیت متصل**:
قابلیتی که عملکرد واقعی آن ساخته و از طریق دریچه قابلیت به موتور اعمال قوانین متصل شده است.
_پرهیز_: قابلیت عمومی

**پلن آزمایشی اختصاصی**:
پلن سفارشی که به کاربران آشنای منتخب متصل می‌شود تا قابلیت‌های متصل‌شده را پیش از عرضه عمومی آزمایش کنند.
_پرهیز_: پلن عمومی

**عرضه عمومی**:
مرحله‌ای که قابلیت پس از آزمایش با کاربران منتخب، در پلن‌های عمومی در دسترس قرار می‌گیرد.
_پرهیز_: اتصال فنی قابلیت

**وضعیت اتصال**:
وضعیت واقعی آماده‌بودن عملکرد قابلیت که User Application آن را از روی دریچه‌های ثبت‌شده تشخیص و گزارش می‌کند.
_پرهیز_: فیلد دستی اتصال در Admin

## Relationships

- مالک محصول یک **قابلیت** و **تعریف قابلیت** آن را کاملاً از طریق Admin مدیریت می‌کند.
- هر **قابلیت** می‌تواند صفر یا یک **عملکرد قابلیت** در User Application داشته باشد.
- هر **عملکرد قابلیت** می‌تواند یک یا چند **عملیات اجرایی** ارائه کند.
- هر **عملیات اجرایی** دقیقاً یک **قابلیت** اصلی برای دسترسی، قیمت و ثبت مصرف دارد.
- نیازمندی‌های جانبی یک **عملیات اجرایی** از طریق وابستگی‌های تعریف‌شده میان قابلیت‌ها بررسی می‌شوند، نه با چند قابلیت اصلی هم‌زمان.
- یک اجرای واحد فقط برای قابلیت اصلی خود هزینه ثبت می‌کند، مگر مالک محصول بعداً یک مدل ترکیبی صریح را جداگانه تصویب کند.
- یک **عملکرد قابلیت** از طریق یک **دریچه قابلیت** به **موتور اعمال قوانین** متصل می‌شود.
- User Application فقط **درگاه اعمال قوانین** را می‌شناسد و روش اتصال به Admin یا دیتابیس را مستقیماً نمی‌شناسد.
- **Adapter API اتصال** پیاده‌سازی production درگاه اعمال قوانین است.
- **Module اتصال مدیریتی** ابتدا داخل پروژه Admin ساخته می‌شود ولی Interface آن باید جداسازی آینده را ممکن نگه دارد.
- **API اتصال مدیریتی** تنها Interface مجاز runtime میان User Application و داده‌های مدیریتی است.
- ارتباط User Application با API فقط با **هویت سرویس** کوتاه‌عمر انجام می‌شود.
- درخواست runtime کاربر را با شناسه داخلی دیتابیس معرفی می‌کند؛ شناسه Clerk در Interface اتصال استفاده نمی‌شود.
- کد قابلیت و عملیات فقط توسط Adapter سرور User Application انتخاب می‌شود و از مرورگر پذیرفته نمی‌شود.
- **API اتصال مدیریتی** مرجع نهایی صدور **مجوز اجرا** و مدیریت **رزرو مصرف** است.
- API اتصال مدیریتی فقط از **نسخه منتشرشده قوانین** برای تصمیم runtime استفاده می‌کند.
- تغییرات پیش‌نویس Admin تا زمان انتشار نباید رفتار User Application را تغییر دهند.
- انتشار قوانین به‌تنهایی User Application را تغییر نمی‌دهد؛ مالک محصول باید **همگام‌سازی دستی قوانین** را اجرا کند.
- مالک محصول می‌تواند یک **بسته همگام‌سازی** انتخاب کند و انتقال آن باید کاملاً atomic باشد.
- پیش از تأیید همگام‌سازی، **پیش‌نمایش همگام‌سازی** نمایش داده می‌شود.
- هر بسته موفق باید امکان **بازگردانی قوانین** به نسخه قبلی را داشته باشد.
- مالک محصول می‌تواند **توقف هنگام به‌روزرسانی** را برای قابلیت مورد اصلاح روشن یا خاموش کند.
- وقتی توقف روشن است، Admin باید وضعیت توقف را واضح و دائماً قابل مشاهده نشان دهد.
- وقتی توقف روشن است، درخواست تازه کاربران مشمول رد می‌شود و پیام «در حال به‌روزرسانی هستیم؛ کمی بعد دوباره تلاش کنید» دریافت می‌کنند.
- وقتی توقف روشن نیست، نسخه همگام‌شده فعلی بدون تغییر ادامه می‌یابد تا نسخه تازه صریحاً همگام شود.
- پس از همگام‌سازی بدون توقف، quoteهای تازه از قیمت جدید استفاده می‌کنند و quote تک‌مصرف موجود همچنان به نسخه قوانین ثبت‌شده خود وابسته است.
- API اتصال از **نسخه قرارداد اتصال** استفاده می‌کند و در زمان ارتقا، نسخه قبلی را تا تکمیل به‌روزرسانی User Application پشتیبانی می‌کند.
- هر عملیات هزینه‌دار یا مصرفی باید **شناسه عملیات** داشته باشد.
- User Application حق محاسبه مستقل entitlement، limit یا credit نهایی را ندارد.
- User Application پس از دریافت **مجوز اجرا** فقط عملکرد واقعی را اجرا می‌کند و نتیجه موفق یا ناموفق را برای قطعی‌سازی یا آزادسازی **رزرو مصرف** گزارش می‌دهد.
- اگر ارسال **گزارش نتیجه عملیات** شکست بخورد، User Application آن را retry می‌کند و عملکرد واقعی را دوباره اجرا نمی‌کند.
- نتیجه محصول تا حد امکان **نتیجه دودویی عملیات** است؛ stateهای داخلی فقط برای recovery نگهداری می‌شوند.
- شکست **تلاش خارجی پرهزینه** نباید باعث retry خودکار همان عملکرد واقعی شود.
- در شکست عملکرد، هزینه از کاربر قطعی نمی‌شود و رزرو او آزاد می‌شود، حتی اگر سیستم برای تلاش خارجی هزینه پرداخت کرده باشد.
- **هزینه واقعی عملیات** برای عملیات موفق و ناموفق همیشه ثبت و در Admin قابل مشاهده است.
- هر شکست، مقدار **زیان جذب‌شده شکست** خود را ثبت می‌کند.
- عبور از **سقف retry شکست** فقط هشدار با اهمیت بالا ایجاد می‌کند و به‌تنهایی وضعیت retry را تغییر نمی‌دهد.
- اجازه یا توقف تلاش دوباره فقط با اقدام دستی Admin انجام می‌شود.
- **زیان جذب‌شده شکست** هر کاربر به‌صورت تجمعی و atomic به **سقف زیان کاربر** سنجیده می‌شود.
- رسیدن یا عبور از **سقف زیان کاربر** فقط هشدار با اهمیت بالا ایجاد می‌کند.
- فعال‌کردن یا برداشتن **توقف بررسی کاربر** فقط با اقدام دستی Admin انجام می‌شود.
- موارد عبور از سقف‌ها باید با اهمیت بالا در Admin نمایش داده و برای بررسی انسانی audit شوند.
- سیستم فعلاً حق تغییر خودکار وضعیت کاربر، retry، سقف‌ها یا رفع محدودیت را ندارد.
- در زمان قطعی API، عملیات محدود و هزینه‌دار متوقف می‌شوند؛ اطلاعات نمایشی می‌تواند از cache استفاده کند.
- cache اطلاعات نمایشی فقط snapshot دستی همگام‌شده را می‌خواند و می‌تواند با بازه فنی قابل تنظیم refresh شود؛ refresh حق publish یا sync قوانین تازه را ندارد.
- **افزونه آزمایشی** قابلیت‌های پایلوت را بدون تغییر پلن اصلی به کاربران منتخب می‌دهد.
- Admin برای موارد قابل تخصیص، **قانون تعلق در بدو ورود** را تعیین می‌کند و User Application حق فرض خودکار در این مورد ندارد.
- User Application یک **فهرست اتصال Runtime** امن ارائه می‌کند و Admin آن را به‌صورت دوره‌ای بررسی و نمایش می‌دهد.
- قابلیت منتشرشده ولی متصل‌نشده قابل اجرا نیست و در Frontend یا مخفی می‌شود یا غیرفعال با پیام «به‌زودی فراهم خواهد شد» نمایش داده می‌شود.
- قابلیت متصل‌نشده برای کاربران عادی مخفی است و برای کاربران آزمایشی غیرفعال با پیام «به‌زودی فراهم خواهد شد» نمایش داده می‌شود.
- **مجوز زمان‌دار** عملیات جاری را تا پایان اعتبارش حفظ می‌کند؛ غیرفعال‌شدن قابلیت درخواست‌های جدید را رد می‌کند.
- عملیات طولانی برای تمدید **مجوز زمان‌دار** پیام زنده‌بودن ارسال می‌کند.
- مصرف بیشتر از رزرو فقط تا **سقف اعتبار منفی** همان پلن می‌تواند مجاز شود؛ پایین‌تررفتن از آن سقف ممنوع است.
- هزینه هر عملیات همیشه پیش از اجرای واقعی به‌صورت **پیش‌فاکتور عملیات** محاسبه و به کاربر نمایش داده می‌شود.
- هر **پیش‌فاکتور عملیات** به **اثر انگشت عملیات** قفل می‌شود.
- تعویض فایل، تغییر محتوا، تنظیمات، عملیات، کاربر یا نسخه قوانین، پیش‌فاکتور قبلی را فوراً نامعتبر می‌کند.
- اعتبار تجاری پیش‌فاکتور به زمان وابسته نیست؛ اعتبار آن state-based و وابسته به یکسان‌ماندن کامل مسیر اجرا است.
- هر پیش‌فاکتور **پیش‌فاکتور تک‌مصرف** است و replay آن برای فایل یا اجرای دیگر ممنوع است.
- عملیات هزینه‌دار فقط پس از **تأیید پیش‌فاکتور** اجرا می‌شود؛ عملیات رایگان به تأیید قیمت نیاز ندارد.
- هنگام استفاده از **سقف اعتبار منفی**، مقدار بدهی و هشدار آن به کاربر نمایش داده می‌شود و رسیدن به سقف، عملیات جدید را متوقف می‌کند.
- لغو، timeout یا شکست باید رزرو مصرف را آزاد کند و recovery رزروهای فراموش‌شده را اصلاح کند.
- عملیات می‌تواند **شناسه منبع عملیات** بدهد تا اجرای تکراری هم‌زمان روی همان منبع متوقف شود.
- پاسخ‌ها و logها از **کد دلیل**، شناسه درخواست و شناسه داخلی استفاده می‌کنند و email، token، URL فایل و PII را ثبت نمی‌کنند.
- **کد پایدار قابلیت** پس از استفاده تغییر یا حذف نمی‌شود؛ نام نمایشی قابل تغییر است و قابلیت فقط archive می‌شود.
- هر کاربر دقیقاً یک **پلن اصلی** دارد و دسترسی اضافه با افزونه‌ها داده می‌شود.
- هزینه فقط پس از تولید **نتیجه معتبر** قطعی می‌شود.
- اگر خرابی نتیجه پس از قطعی‌شدن هزینه کشف شود، سیستم **پرونده بازگشت اعتبار** با شواهد لازم ایجاد می‌کند.
- refund کامل، refund جزئی یا رد درخواست فقط با تصمیم دستی Admin انجام می‌شود.
- هر تصمیم refund باید idempotent، auditشده و متصل به عملیات و ledger اصلی باشد.
- انتشار، همگام‌سازی و بازگردانی فقط برای مدیران مجاز است و تمام این عملیات audit می‌شوند.
- مدت نگهداری مصرف، اعتبار، پیش‌فاکتور، عملیات و گزارش‌ها از Admin قابل تنظیم است؛ مقدارهای پیش‌فرض هنوز تعیین نشده‌اند.
- Admin برای شکست همگام‌سازی، رزرو گیرکرده و افزایش شکست‌های خارجی پرهزینه هشدار دریافت می‌کند.
- تمام عملیات محدود یا هزینه‌دار فعلی و آینده باید از **درگاه اعمال قوانین** عبور کنند و bypass مجاز نیست.
- Roadmap باید بر اساس Moduleهای عمیق و اتصال کامل مرحله‌ای نوشته شود، نه یک task برای هر جدول یا تابع کوچک.
- اولین اثبات معماری یک قابلیت آزمایشی ساختگی end-to-end است؛ پس از آن Analyze اولین قابلیت واقعی متصل‌شده خواهد بود.
- رفع مشکلات امنیتی بحرانی فعلی نباید منتظر تصمیم‌های تجاری یا تکمیل موتور قابلیت بماند.
- User Application در production نباید credential دیتابیس مدیریتی یا دسترسی مستقیم به جدول‌های آن داشته باشد.
- Routeها و عملکردهای قابلیت حق دسترسی مستقیم به جدول‌های مدیریتی را ندارند.
- یک **قابلیت تعریف‌شده** می‌تواند پیش از وجود عملکرد واقعی در Admin باقی بماند.
- یک **قابلیت متصل** لزوماً عمومی نیست و می‌تواند فقط در یک **پلن آزمایشی اختصاصی** قرار گیرد.
- User Application منبع حقیقت **وضعیت اتصال** است و Admin فقط آن را نمایش می‌دهد.
- مالک محصول نمی‌تواند **وضعیت اتصال** را به‌صورت دستی متصل اعلام کند.
- یک **پلن آزمایشی اختصاصی** به کاربران آشنای منتخب متصل می‌شود.
- **عرضه عمومی** با قراردادن قابلیت آزمایش‌شده در پلن‌های عمومی انجام می‌شود.
- تغییر پلن، قیمت، محدودیت، وابستگی، فعال‌بودن یا حذف از پلن نباید نیازمند تغییر **عملکرد قابلیت** یا **موتور اعمال قوانین** باشد.
- ساخت یک رفتار نرم‌افزاری واقعاً جدید نیازمند پیاده‌سازی **عملکرد قابلیت** و اتصال آن به **دریچه قابلیت** است.

## Example dialogue

> **مالک محصول:** «قابلیت تازه را در Admin ساختم، برایش هزینه اعتباری گذاشتم و به دو پلن اضافه کردم.»
> **توسعه‌دهنده:** «من فقط عملکرد واقعی آن را می‌سازم و به دریچه قابلیت وصل می‌کنم؛ موتور پلن و اعتبار تغییر نمی‌کند.»
>
> **مالک محصول:** «قابلیت متصل شد؛ فعلاً آن را فقط در پلن آزمایشی کاربران آشنا می‌گذارم و بعد از تأیید عمومی می‌کنم.»

## Flagged ambiguities

- «ساخت قابلیت» قبلاً هم برای تعریف مدیریتی و هم برای کدنویسی رفتار واقعی استفاده می‌شد؛ از این پس **تعریف قابلیت** و **عملکرد قابلیت** دو مفهوم جدا هستند.
- وضعیت اتصال از فهرست امن Runtime User Application می‌آید و Admin آن را نمایش می‌دهد.
- پرسش‌های طراحی داخلی Admin فقط وقتی وارد محدوده می‌شوند که برای اتصال لازم باشند یا مالک محصول صریحاً آن‌ها را تأیید کند.
- cache قوانین به‌صورت خودکار فعال نمی‌شود؛ مالک محصول همگام‌سازی را دستی آغاز می‌کند.
- «بازه تخفیفی» برای موجودی منفی استفاده شد؛ اصطلاح قطعی **سقف اعتبار منفی** است و با تخفیف تفاوت دارد.
- شیوه درخواست مصرف اضافه در محدوده سقف اعتبار منفی هنوز باید دقیق شود.
- مصداق‌های دقیق قابل تنظیم با **قانون تعلق در بدو ورود** باید هنگام بازنویسی قرارداد Admin مشخص شوند.
- فیلدهای دقیق **اثر انگشت عملیات** و روش اثبات immutability منبع هنوز باید قطعی شوند.
- timeout فنی cleanup برای پیش‌فاکتورهای رهاشده نباید قیمت یا اجازه اجرای کاربر را تعیین کند و هنوز باید جداگانه طراحی شود.
- بازه، scope و مقدار سقف‌ها از Dashboard مدیریت می‌شوند و تصمیم خودکار runtime محسوب نمی‌شوند.
- اثر **توقف هنگام به‌روزرسانی** بر عملیات از قبل مجازشده و در حال اجرا هنوز باید در طراحی فنی با قانون مجوز زمان‌دار سازگار شود.
